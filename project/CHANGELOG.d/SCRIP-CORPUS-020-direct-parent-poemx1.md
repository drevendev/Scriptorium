# SCRIP-CORPUS-020 — direct parent `poemx1` extraction prerequisite

- Added a fail-closed replay for the seven direct parent-level `poemx1` calls already frozen by the four-parent Klim Samgin body-surface contract: five in Part 1, two in Part 2, none in Parts 3–4.
- Re-opened only the exact parameter-2 values in process memory and classified the conservative MediaWiki-core and Poem branches that can affect their visible text; literary prose is not persisted.
- All seven values are plain under the bounded candidate-specific surface: aggregate input is 1,184 characters / 2,136 UTF-8 bytes with 34 newlines, zero active MediaWiki-core constructs, zero unresolved invocations, and no reached Poem indentation, leading-space, horizontal-rule, or trim branch.
- Persisted source-free per-invocation parameter identities and inferred post-unstrip Poem fragment identities. This establishes that future fail-closed literary extraction may replace these seven exact direct calls with their verified parameter-2 literary values without introducing a generic MediaWiki parser.
- Kept the evidence class `candidate_specific_inferred_reconstruction`. Exact historical Russian Wikisource MediaWiki-core/Poem deployment equivalence remains unproven.
- No per-part literary body or four-part composite digest is claimed; FantLab analyzer-input identity remains unknown and M2 remains 0/5.
