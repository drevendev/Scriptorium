# SCRIP-CORPUS-049 — freeze `{{ё}}` version-pinned replay contract

- Official MediaWiki `API:Expandtemplates/ru` permanent revision `oldid=6729113` is retained as source-free API evidence: `revid` supplies revision context for `{{REVISIONID}}` and similar variables; it is not treated as a historical transcluded-template version pin.
- TemplateSandbox title/text substitution is recorded as a useful direct override but not as proof that a recursively expanded dependency graph is closed; recursive-expansion provenance is now pinned to MediaWiki `Help:ExpandTemplates` permanent revision `oldid=8168760` (2026-01-23).
- Independent review `5266226440` found that the first closure validator could accept both roots with no evidence that nested dependencies had actually been enumerated. The repaired contract now requires per-node source-free discovery proof (`status=complete`, discovery method, exact direct-dependency titles and evidence SHA-256) before `require_complete=True` can pass.
- For a complete closure, every discovered child must itself be bound and the explicit graph edges must exactly equal the discovered direct-dependency relationships. This distinguishes an evidenced leaf from an undiscovered child and fails closed on incomplete discovery.
- The replay contract still requires exact source-free identities for `Шаблон:ё`, `Шаблон:ЕЁ` and every nested template/module dependency: title, revision ID, revision timestamp and MediaWiki content SHA-1. Any live/unbound dependency keeps the closure open.
- Canonical contract schema `scriptorium-darwin-template-yo-replay-contract-v1` now has SHA-256 `bb67d05c5aed51cc03917638cbd6d49257d51933e0adb88982ac73826849366c`; this unit intentionally binds zero dependency revisions and verifies no replay outputs.
- `{{ё}}` remains unresolved. The render profile/backlog are unchanged; renderer/body/>=300k/FantLab/diagnostic/M2 gates remain closed and M2 remains 0/5.
