# SCRIP-CORPUS-049 — freeze `{{ё}}` version-pinned replay contract

- Official MediaWiki `API:Expandtemplates/ru` permanent revision `oldid=6729113` is retained as source-free API evidence: `revid` supplies revision context for `{{REVISIONID}}` and similar variables; it is not treated as a historical transcluded-template version pin.
- TemplateSandbox title/text substitution is recorded as a useful direct override but not as proof that a recursively expanded dependency graph is closed; recursive-expansion provenance is pinned to MediaWiki `Help:ExpandTemplates` permanent revision `oldid=8168760` (2026-01-23).
- Independent review `5266226440` found that the first closure validator could accept both roots with no evidence that nested dependencies had actually been enumerated. The repaired contract requires per-node source-free discovery proof (`status=complete`, discovery method, exact direct-dependency titles and evidence SHA-256) before `require_complete=True` can pass.
- Independent review `5267466282` found that the discovery SHA-256 was still only shape-checked. The validator now recomputes a canonical source-free digest over candidate identity, exact node title/revision ID/revision timestamp/MediaWiki SHA-1, discovery status/method and the exact direct-dependency list. Arbitrary digests and stale digests after revision/dependency mutation fail closed.
- For a complete closure, every discovered child must itself be bound and the explicit graph edges must exactly equal the cryptographically bound direct-dependency relationships. This distinguishes an evidenced leaf from an undiscovered child and fails closed on incomplete or stale discovery evidence.
- The replay contract still requires exact source-free identities for `Шаблон:ё`, `Шаблон:ЕЁ` and every nested template/module dependency. Any live/unbound dependency keeps the closure open.
- Canonical contract schema `scriptorium-darwin-template-yo-replay-contract-v1` now has SHA-256 `35bed756f4fafa4f443315c09d04a35c66441eb2249eced3c7540ac20c981afe`; discovery digests use schema `scriptorium-template-dependency-discovery-evidence-v1`. This unit intentionally binds zero dependency revisions and verifies no replay outputs.
- `{{ё}}` remains unresolved. The render profile/backlog are unchanged; renderer/body/>=300k/FantLab/diagnostic/M2 gates remain closed and M2 remains 0/5.
