# SCRIP-CORPUS-065 — Darwin `{{ё}}` root direct-dependency discovery

- Added a fail-closed, candidate-bounded transclusion dependency scanner for the two exact Russian Wikisource replay roots already bound by v3. It uses exact-revision content only transiently and retains only byte/digest metadata plus dependency titles.
- An initial `action=parse&oldid&prop=templates` probe was rejected as unsuitable evidence because it surfaced documentation/noinclude transclusions. The accepted path instead verifies exact source SHA-1 first, applies transclusion controls, and scans only static template/`#invoke` dependencies; dynamic or unsupported names fail closed.
- Independent review `5285711348` caught a correctness defect before merge: the first scanner version treated every uppercase bare invocation as a magic word, so the exact transclusion-visible `{{ЕЁ|ё|е}}` call was incorrectly dropped. The repair removes that heuristic, pins an explicit MediaWiki `Help:Magic words@8589537` evidence surface, binds exact uppercase `ЕЁ` as a template, and fails closed on unclassified uppercase or parameterized magic-word-like invocations.
- Corrected frozen exact observations: `Шаблон:Ё@5687302` is 271 UTF-8 bytes with transclusion SHA-256 `bd7465151d984f06c75eae0c1246c26a0643826a26a170a3e9a9d43c9e5a03b7` and direct dependency `Шаблон:ЕЁ`; `Шаблон:ЕЁ@3684646` is 688 bytes with transclusion SHA-256 `3975fa227e23e8cc1487a49a3eab86b28e0ddb6a753c347c3191ee800ae56026` and directly invokes `Модуль:String`.
- Added `scriptorium-darwin-template-yo-replay-contract-v4`, a narrow successor to reviewed v3 SHA-256 `8caa144d9f4c0b9a8149c1822bd79d168d212dea14d72079c3259d55e5e03c8c`. The corrected v4 freezes complete discovery evidence for both roots and has contract SHA-256 `27bbea3edcf583edb4066611062bcba8b9eb08a3cbbaaf70718800b99bb1e9f6`.
- The bound root edge `Шаблон:Ё -> Шаблон:ЕЁ` is now explicit in the replay graph. The discovered `Шаблон:ЕЁ -> Модуль:String` edge is retained separately as `target_identity_bound=false`; `dependency_closure_complete=false` because the module identity and its recursive dependencies are not yet frozen.
- Added regressions proving `{{ЕЁ|ё|е}} -> Шаблон:ЕЁ`, a documented bare magic word is not treated as a template, and unknown uppercase / parameterized magic-word-like invocations fail closed. Deterministic v4 rebuild/byte-compare verification and the read-only exact-root workflow remain the verification path.
- Corrected the public source-free direct-dependency companion and durable handoff after the review blocker. This repair wake does not self-approve or merge the substantive change; independent exact-head judgement remains required.
- No recursive closure, forced/non-forced output verification, renderer promotion, literary-body identity, >=300k admission, FantLab source match, or M2 gate changed. M2 remains 0/5.
