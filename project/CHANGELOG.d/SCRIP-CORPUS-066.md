# SCRIP-CORPUS-066 — Darwin dependency revision-selection policy

- Selected the next bounded Darwin/Rachinsky provenance unit after reviewed v4 left `Модуль:String` exact-identity-unbound. Before choosing a child revision, the unit resolves what evidence is allowed to select that revision.
- MediaWiki `Help:Templates` documents ordinary template calls as dynamic transclusion: template edits affect pages that transclude them when those pages are loaded. `API:Expandtemplates` documents `revid` as revision context for `REVISIONID` and similar variables, not as a recursive template/module version pin. Wikimedia Phabricator T31051 records the lack of normal versioned transclusion support; T70399 was closed as its duplicate on 2026-08-14.
- Added source-free `scriptorium-darwin-dependency-revision-selection-policy-v1`, bound to independently reviewed v4 SHA-256 `f5a021fe544bb74f8f7a493bf20c7005f5a79c9c7b78eb10c86ff93a86b26e64`. Policy SHA-256 is `4a58b70d5935f4455e58a929c6c1b6cc6a527d7b9d63121e0023501055889855`.
- The policy explicitly forbids selecting a dependency revision from either caller/root timestamp and forbids treating `expandtemplates revid` as a transitive version pin. The next `Модуль:String` binding must record canonical title, exact revision ID, revision timestamp, MediaWiki SHA-1 and the controlled observation context together.
- Added a fail-closed validator and regressions for the policy digest, caller-timestamp rejection, `expandtemplates revid` rejection and unchanged downstream gates. Extended the existing read-only Darwin dependency workflow to run those checks on PR heads.
- Updated the public source-free direct-dependency companion so visitors can see why the module is intentionally still unbound instead of being backdated from a root timestamp.
- Opened Issue #219 and Draft PR #220. This authoring wake does not self-approve or merge the substantive change; independent exact-head judgement remains required after checks settle.
- No `Модуль:String` identity, recursive closure, forced/non-forced output verification, renderer promotion, literary-body identity, >=300k admission, FantLab source match or M2 gate changed. M2 remains 0/5.
