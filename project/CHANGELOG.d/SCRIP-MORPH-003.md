## 2026-09-14 — Hosted pinned pylem execution candidate

- Re-checked the highest-priority `SCRIP-MORPH-003` path before selection. The local execution runtime still has no installed `pylem`, and DNS resolution for `github.com`, `pypi.org`, and `files.pythonhosted.org` still fails; that local condition remains infrastructure `not_run`, not a pylem failure.
- Opened Issue #54 and moved the bounded unit onto a dedicated GitHub Actions execution lane rather than skipping morphology again now that reviewed hosted workflow infrastructure exists.
- Added `scriptorium.pylem_provider`, a lazy adapter that requires installed distribution version `pylem==0.0.18`, aligns every `LemmaInfo.part_of_speech` candidate to `scriptorium-text-v1` word tokens, preserves candidate order/multiplicity, performs no homonym selection, and fails closed on runtime POS strings outside the pinned AOT mapping vocabulary.
- Kept runtime `N` and the five extra AOT categories unresolved. The adapter does not infer noun/cardinal identity, fold extra categories, or claim FantLab dictionary equivalence.
- Added `requirements/pylem-0.0.18.txt` with PyPI's published source-distribution SHA-256 `66c0d13414006a803f200979540fcee9653738701c14496a418db67542b26515` and a read-only hosted workflow that installs with `--require-hashes --no-deps`, runs the complete standard-library suite, executes a tiny Russian native smoke, and uploads only a source-free runtime receipt.
- Added deterministic provider-boundary tests using a fake holder so the normal suite remains independent of native pylem installation. The tests cover candidate preservation, missing analyses, unresolved runtime codes, unknown/blank fail-closed behavior, exact package hash, exact PR-head checkout, workflow permissions, and absence of secrets.
- The native receipt deliberately records `fantlab_dictionary_equivalence=unknown`, unresolved homonym and noun/cardinal semantics, and `m2_parity_admissible=false`. This slice can establish provider execution provenance only; it cannot advance the 0/5 source-matched M2 gate.
- Exact hosted build/runtime evidence is required on the final authored PR head before independent review. The full frozen-work POS diagnostic wiring remains a later continuation of `SCRIP-MORPH-003`, after the provider lane itself is demonstrated healthy.
