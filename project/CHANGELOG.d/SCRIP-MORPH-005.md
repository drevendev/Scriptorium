## 2026-09-15 — Full-work POS undefined/delta decomposition candidate

- Opened Issue #58 / PR #59 for `SCRIP-MORPH-005` after SCRIP-MORPH-004 merged and no recovery/review-ready work remained.
- Added `scriptorium-pos-undefined-diagnostic-v1`, a source-free diagnostic over already validated exact-pylem runtime candidate rows. It does not recover `N`, fold extra AOT categories, select homonyms, or change `scriptorium-pos-v1` semantics.
- Every undefined token is placed in exactly one reason class: `no_analysis`, `runtime_n_only`, `runtime_n_mixed`, `extra_runtime_only`, `extra_runtime_mixed`, or `direct_cross_bucket_ambiguity`. Separate token-presence tables expose unresolved runtime codes and direct FantLab-shaped bucket candidates without publishing token text.
- Added fail-closed FantLab POS partition accounting. Both the FantLab and Scriptorium word/defined/undefined partitions must sum exactly before deltas are emitted; all such deltas remain `diagnostic_only` because analyzer-input identity is unknown.
- Versioned the frozen artifact as `scriptorium-frozen-pos-diagnostic-v2`; bucket rows now explicitly state that actual counts cover only `scriptorium_conservatively_defined_tokens_only`.
- Hosted full-work evidence on authored head `4ceee1baa4b961960d32addf878fcef5a2ecf809` reproduced 269,358 current Scriptorium words, 117,554 defined and 151,804 undefined tokens. Of the undefined tokens, only 2 are `no_analysis`; 151,802 (99.9987%) are withheld by known conservative policy boundaries.
- The mutually exclusive reason counts are: 55,081 `runtime_n_only`, 19,670 `runtime_n_mixed`, 10,677 `extra_runtime_only`, 7,120 `extra_runtime_mixed`, 59,254 `direct_cross_bucket_ambiguity`, and 2 `no_analysis`.
- Non-exclusive unresolved runtime-code presence is material: `N` occurs in 74,751 undefined token rows, `ADJ_SHORT` in 10,126, `INFINITIVE` in 8,230, `PARTICIPLE_SHORT` in 1,100, `COLLOC` in 350 and `POSL` in 0. These are diagnostic candidate-presence counts, not justified FantLab folds.
- FantLab partition accounting for the unmatched frozen candidate is 253,275 expected words versus 269,358 actual (+16,083), 188,239 expected defined POS words versus 117,554 actual (-70,685), and 65,036 expected undefined POS words versus 151,804 actual (+86,768). The deltas combine source/tokenization differences with conservative morphology policy and cannot be attributed to FantLab behavior.
- The strongest conclusion is negative/structural: provider dictionary/prediction coverage is not the reason for almost all current undefined tokens. The remaining gap is dominated by deliberately unresolved `N`, extra-category and homonym-selection boundaries. This narrows future work without inventing their resolution.
- The source edition behind FantLab work 270306 remains unknown; FantLab dictionary identity, homonym selection, noun/cardinal recovery, extra-category folding and service-word aggregation remain unresolved. M2 remains 0/5 source-matched works.
- PR #59 is an authored substantive change and is intentionally left unmerged for an independent later exact-head review. Updating durable state after authored verification creates a newer head, so the reviewer must require fresh exact-final-head workflows and inspect the v2 source-free artifact before merge.
