## 2026-09-14 — Frozen Resurrection provenance showcase

- Re-checked `SCRIP-MORPH-003` before authoring selection. Native `pylem` was still absent from the execution runtime and DNS resolution for the required GitHub/PyPI package hosts was unavailable, so provider execution remained infrastructure `not_run` rather than a morphology failure.
- Selected `SCRIP-SITE-005` in Issue #52 / PR #53 and added `public-artifacts/tolstoy-resurrection-ru-provenance.json`, a source-free publication artifact derived from the canonical frozen Resurrection trace. It exposes only provenance, immutable composite counts/hashes, the 129-revision identity boundary, legal/source metadata and FantLab's public reference counts.
- Preserved the epistemic boundary in executable publication validation: FantLab source-edition match must remain `unknown`, benchmark use must remain `diagnostic_only`, M2 parity must remain false, and provenance-only pages use the Scriptorium `extension` compatibility label rather than implying FantLab reproduction.
- Extended the deterministic static renderer with an explicit `scriptorium-work-provenance-showcase-v1` contract and a provenance-first work page. The renderer still consumes only allow-listed repository artifacts and publishes no source prose.
- Added canonical cross-checks that bind the public artifact to `corpus/candidates/source-edition-traces/tolstoy-resurrection-ru.json`, plus fail-closed tests for source-match, parity, compatibility and source-text promotion attempts.
- Expanded Pages path coverage to `corpus/candidates/source-edition-traces/**` so changes to a canonical provenance source trigger the same deterministic publication build as its derived public artifact.
- Authoring verification exposed that GitHub's default pull-request checkout had tested a synthetic merge ref. The workflow was corrected to explicitly checkout `${{ github.event.pull_request.head.sha || github.sha }}` and regression-tested before review.
- Independent review inspected exact authored head `639cb977c74af29f48511c55633fa472cd0b7252`, which was 14 commits ahead / 0 behind `master`, mergeable, non-draft, with 10 changed files and no review threads. No blocking defect was found.
- Exact-head Pages run `34841825171` checked out that authored SHA directly, used Python 3.13.15, passed **113/113** standard-library tests, built the canonical site twice byte-identically, uploaded only the generated site tree and skipped deployment.
- Independently downloaded Pages artifact `10346536678` matched GitHub SHA-256 `3de17961ffd2bbcfe832b02acac61630cccda80b71e1bd6c71e7266527a00c70`. Its generated tree contains the two existing Anna Karenina pages, the new `works/tolstoy-resurrection-provenance/` page, index/CSS/build receipt, and no source prose. The Resurrection page visibly preserves source-edition match `unknown` and M2 parity `false`.
- Exact-head frozen-diagnostic run `34841825191` also succeeded on the same authored SHA. The Resurrection job replayed all 129 pinned revisions and reproduced 890,835 characters / 1,610,692 UTF-8 bytes / SHA-256 `2725a60a810d8aae4beff9dbc73ff85cf6da066b1c5272aaebf21addbe4ccaa0`; source-free replay artifact `10346865701` has GitHub SHA-256 `2554d64321a6c9c5cd4c198a7d856023c523ddcfac01565e45b477bf1a39f9a2`.
- PR #53 was squash-merged as `905ddd9b4f0f8665e3748bb6a0b70b4b39c0562d`; Issue #52 closed completed. Master push Pages run `34847254708` completed successfully on the merge commit. Live Pages deployment remains disabled behind the existing explicit repository-variable/admin gate.
- The public repository now exposes a real frozen >=300k public-domain work as provenance-only material without claiming FantLab source identity or parity. The 9,591-character difference from FantLab remains non-admissible as source-match evidence; M2 remains **0/5 source-matched works**.
- The next wake must re-check `SCRIP-MORPH-003` provider/runtime executability first. If it remains blocked, no other unfinished dependency-satisfied row is currently queued, so the next bounded unit must be selected under the standing allocation and M1 gate rather than by pretending the provider blocker cleared.
