# Run receipt — SCRIP-CORPUS-020 bounded Poem render surface

Date: 2026-09-18  
Issue: #109  
Draft PR: #121  
Base at selection: `44be9f7d3d29a5c5fe06eb67313047bb28624f4c`

## Selected bounded unit

Close only the candidate-specific MediaWiki-core recursive-parse / Poem transformation prerequisite for the six already-frozen plain `poemx1` parameter-2 values. Do not build a generic MediaWiki parser, do not resolve the full Part 2 body in the same unit, and preserve the unknown historical Wikisource deployment boundary.

## Evidence and result

The replay re-fetches pinned Part 2 dependency oldid `2366546`, validates the exact six invocation bindings plus the committed parameter-2/tag/Poem-anchor manifests, and re-opens each literary value only in memory. All six values have zero counts for the conservative `Parser::internalParse` active-syntax inventory recorded by the new artifact. The Poem-specific indentation, leading-space, horizontal-rule and trim branches are also unreached.

Under the already-frozen zero-attribute `#tag:poem` call and inferred upstream Poem anchor `03b3694613e23efa1254d8bbbb98121efaef2cb0`, nine source newlines therefore take the protected-break path. The six source-free post-unstrip fragment identities total 534 characters / 799 UTF-8 bytes. The committed artifact stores per-fragment counts/digests but no literary prose.

Initial exact-head workflow `35304974139` on `abf70f43291dae21c718a6d1df5c18d3d876bcff` passed the full standard-library suite, exact-revision re-fetch, bounded reconstruction and artifact upload. The generated artifact was then committed so later exact-head runs must byte-compare replay output against repository evidence.

## Boundary

This is `candidate_specific_inferred_reconstruction`, not historical-render proof. The Poem source commit is not proven to be the deployed Russian Wikisource extension revision, and the cited MediaWiki core source defines the conservative parser-stage surface rather than identifying the historical deployed core revision. `resolved_part2_identity_frozen=false`, `fantlab_source_edition_match=unknown`, and M2 remains 0/5.

## Next trigger

After independent later-run review/merge of PR #121, continue Issue #109 by resolving the pinned target-only Part 2 expansion end-to-end with these now-frozen six render fragments, then define fail-closed per-part extraction and deterministic 1 -> 2 -> 3 -> 4 composition before recording any composite literary-body digest.
